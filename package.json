{
  "name": "whatsapp-bot-leve",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --optimize-for-size --max-old-space-size=480 bot.js",
    "clean": "rm -rf sessions && mkdir sessions"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.24.2",
    "qrcode-terminal": "^0.12.2",
    "pino": "^8.17.0",
    "express": "^4.18.2",
    "memory-cache": "^0.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
